version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      - DEBUG=0
    volumes:
      - media_data:/app/media
    restart: unless-stopped

volumes:
  media_data:
